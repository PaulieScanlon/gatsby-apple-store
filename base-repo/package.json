{
  "name": "base-repo",
  "version": "1.0.0",
  "description": "Base repo for Gatsby Cloud configuration",
  "main": "index.js",
  "scripts": {
    "build": "yarn workspace apple-store-uk build && yarn workspace apple-store-us build && yarn workspace style-guide build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
